var dataStorage = require("./module1");

dataStorage.collectData(2) // calls the collect data function from our module1 file and inputs
dataStorage.collectData(4)
dataStorage.collectData(57)
dataStorage.collectData(34)
dataStorage.collectData(3)

dataStorage.sortedDataList()  //calls our sortedDataList from module1 file


// Implement a Node.js script that imports the functionality of your module, adds
//  at least 5 different data points to the module's data list, and outputs the sorted list.